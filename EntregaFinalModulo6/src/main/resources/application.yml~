spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/universidad_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate  # Cambiado de update a validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
logging:
  level:
    com.bufigol.universidad.servicio: DEBUG